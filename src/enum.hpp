#ifndef ENUM_H_
#define ENUM_H_

enum module_state {
  m_idle, m_busy, m_error
};

enum volume {
  v0, v1, v2, v3, v4, v5, v6, v7, v8, v9
};

enum speed {
  s0, s1, s2, s3, s4, s5, s6, s7, s8, s9
};

enum tone {
  t0, t1, t2, t3, t4, t5, t6, t7, t8, t9
};

enum ring {
  r1, r2, r3, r4, r5
};

enum msg {
  m1, m2, m3, m4, m5
};

enum alert {
  a1, a2, a3, a4, a5
};

#endif // ENUM_H_
